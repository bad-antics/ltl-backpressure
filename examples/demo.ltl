// examples/demo.ltl — runnable demo for ltl_backpressure

import std::io
use ltl_backpressure as lib

fn main() {
    io::println("ltl_backpressure demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}